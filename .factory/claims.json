[
  {
    "id": "capacity-forecast",
    "claim": "Forecasts remaining useful sessions and warns when a source may run out before reset.",
    "where": "landing preview, demo ledger, README",
    "test": "npm run test:e2e -- --grep @claim:capacity-forecast",
    "sandbox": "fresh browser context at /demo with bundled sample sources"
  },
  {
    "id": "csv-export",
    "claim": "Exports capacity and project spend as CSV.",
    "where": "landing facts, ledger toolbar, README",
    "test": "npm run test:e2e -- --grep @claim:csv-export",
    "sandbox": "fresh browser context at /demo; assert headers and all seven sample rows"
  },
  {
    "id": "csv-import",
    "claim": "Imports valid capacity source readings from CSV.",
    "where": "landing facts, ledger toolbar, README",
    "test": "npm run test:e2e -- --grep @claim:csv-import",
    "sandbox": "fresh browser context at /demo; import one valid row and assert its capacity and cost"
  },
  {
    "id": "project-spend",
    "claim": "Records cost by project and updates the attributed-spend total.",
    "where": "landing steps, demo ledger, README",
    "test": "npm run test:e2e -- --grep @claim:project-spend",
    "sandbox": "fresh browser context at /demo; add one project cost and assert the row and recalculated percentage"
  },
  {
    "id": "demo-isolation",
    "claim": "Demo changes never enter the real workspace.",
    "where": "demo banner, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:demo-isolation",
    "sandbox": "fresh browser context; edit demo then enter real ledger and assert empty state"
  },
  {
    "id": "prompt-privacy",
    "claim": "The demo collects no prompts and sends no data to another origin.",
    "where": "landing facts, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:prompt-privacy",
    "sandbox": "record every request during reset and export in a fresh /demo context"
  },
  {
    "id": "data-boundary",
    "claim": "Product screens never request prompts, source code, API keys, or vendor passwords, and the ledger API rejects extra fields.",
    "where": "landing boundaries, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:data-boundary",
    "sandbox": "fresh real ledger; inspect form labels and verify an API payload containing a prompt is rejected and not stored"
  },
  {
    "id": "server-persistence",
    "claim": "Real workspaces save and load through the shared durable database service.",
    "where": "README",
    "test": "npm run test:e2e -- --grep @claim:server-persistence",
    "sandbox": "random isolated workspace through the local container API; Rust integration coverage opens independent app instances against one database file"
  },
  {
    "id": "workspace-sharing",
    "claim": "A private workspace link opens the same saved team ledger.",
    "where": "ledger toolbar, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:workspace-sharing",
    "sandbox": "seed a random workspace, open its link in a fresh browser context, and assert its source and team name"
  },
  {
    "id": "offline-queue",
    "claim": "An open ledger keeps edits on the device during connection loss and saves them after reconnect.",
    "where": "ledger offline notice, handoff",
    "test": "npm run test:e2e -- --grep @claim:offline-queue",
    "sandbox": "fresh real workspace; go offline, add a source, reconnect, and read the saved source from the API"
  },
  {
    "id": "rate-limit",
    "claim": "The API limits bursts and returns Retry-After with HTTP 429.",
    "where": "README, handoff",
    "test": "npm run test:e2e -- --grep @claim:rate-limit",
    "sandbox": "60 parallel reads from one synthetic forwarded client IP"
  },
  {
    "id": "paid-license",
    "claim": "A returned team license is stored, removed from the URL, and verified with Sociobot.",
    "where": "team plan panel, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:paid-license",
    "sandbox": "fresh browser context with a recorded valid Sociobot verification response"
  },
  {
    "id": "license-daily-cache",
    "claim": "An unchanged license is verified with Sociobot no more than once a day.",
    "where": "privacy page",
    "test": "npm run test:e2e -- --grep @claim:license-daily-cache",
    "sandbox": "fresh browser context with a recorded valid response; reload and assert one verification request"
  },
  {
    "id": "source-cap",
    "claim": "The free ledger holds three sources, and a valid team license permits more.",
    "where": "landing pricing, ledger plan panel, README",
    "test": "npm run test:e2e -- --grep @claim:source-cap",
    "sandbox": "fresh real ledger; assert the fourth source is blocked free and accepted with a cached valid license"
  },
  {
    "id": "policy-boundary",
    "claim": "The product does not proxy models or provide an account-sharing or credential workflow.",
    "where": "landing boundaries, terms, README",
    "test": "npm run test:e2e -- --grep @claim:policy-boundary",
    "sandbox": "fresh browser context; assert boundary copy, absent password fields, and a 404 from a model-proxy endpoint"
  },
  {
    "id": "team-plan-availability",
    "claim": "The $9 monthly team plan clearly states that checkout is not available and shows no unavailable checkout link.",
    "where": "landing pricing, ledger plan panel, terms, README",
    "test": "npm run test:e2e -- --grep @claim:team-plan-availability",
    "sandbox": "fresh landing page; assert the price, unavailable status, and absence of a buy link"
  }
]
